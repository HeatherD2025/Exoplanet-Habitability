name: Monthly NASA Exoplanet Data Sync

on:
  schedule:
    # Runs at 00:00 UTC on the 1st day of every month
    - cron: '0 0 1 * *'
  workflow_dispatch:

jobs:
  sync-database:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Run Database Sync
        env:
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
          SEED_TARGET: 'supabase'
        run: npx tsx backend/prisma/seed.ts